package com.simongo.service;

import com.simongo.math.Imaginary;

public interface MathService {

    int add(int one, int two);

    int subtract(int one, int two);

    Long add(Long one, Long two);

    Imaginary add(Imaginary one, Imaginary two);

}
